# Limitations

This project's synthetic panel does not replace real user research (see
`README.md`). This file states, from `docs/validity_report.md`'s measured
results, where the panel is useful, where it is not, and what it
systematically misses.

## Where the panel is useful

Reproducibility is real and measured: the same seed, run again, produces the same finding count and the same top-5 cluster membership every time (variance {{ report.reproducibility.finding_count_variance }}, mean top-5 Jaccard overlap {{ report.reproducibility.mean_top5_cluster_jaccard }}). Any disagreement between two runs of the same study configuration is a real signal worth investigating, not measurement noise from the harness itself.

## Where it is not, and what it systematically misses

### 1. Cluster themes partly reflect the Analyst's writing style, not just panel agreement

Near-duplicate paraphrases of one problem cluster at a maximum internal cosine distance of {{ "%.3f"|format(report.clustering_margin.near_duplicate_max_internal_distance) }}; distinct problems phrased in the same uniform "Analyst voice" sit as close as {{ "%.3f"|format(report.clustering_margin.distinct_pair_min_distance) }} apart. The margin between them ({{ "%.3f"|format(report.clustering_margin.margin) }}, against a `distance_threshold` of {{ report.clustering_margin.distance_threshold }}) is real but not large: TF-IDF cosine similarity over Analyst-authored summaries partly measures how uniformly the Analyst writes, not how much the underlying panel actually agrees. A study whose Analyst calls happen to use more uniform phrasing will show tighter, larger clusters than one whose calls don't, independent of what the personas actually reported.

### 2. A rare theme and a broken measurement look identical in the report

Of {{ report.zero_vector_conflation.total_singleton_clusters }} singleton clusters in a targeted fixture, {{ report.zero_vector_conflation.zero_vector_singleton_count }} came from a finding whose summary vectorised to an all-zero TF-IDF row (short or generic enough that every token was excluded before clustering even started), and {{ report.zero_vector_conflation.genuine_frequency_one_singleton_count }} were genuine frequency-1 themes. Nothing in a rendered report tells them apart -- a reader sees "1 persona reported this" either way, whether that means a real, rare finding or a finding the clustering pipeline never had a chance to place.

### 3. Cluster granularity at a fixed threshold is unmeasured territory as studies scale up

At `distance_threshold={{ report.threshold_scaling.distance_threshold }}` (the shipped default, unchanged here), cluster count and mean cluster size across increasing synthetic corpus sizes:

| Finding count | Cluster count | Mean cluster size |
|---|---|---|
{% for p in report.threshold_scaling.points %}
| {{ p.finding_count }} | {{ p.cluster_count }} | {{ "%.2f"|format(p.mean_cluster_size) }} |
{% endfor %}

This threshold was calibrated against a seven-document fixture (§M5); this table is the first measurement of its behaviour at larger, more realistic finding counts. The default is unchanged here -- `distance_threshold` and `sklearn.__version__` are embedded in every report's provenance, and changing the default would break comparability with every report already produced.

## The four provider-gated checks, in full when a real provider measured them

Discriminative validity, acquiescence bias, position bias, and known-answer calibration all depend on the panel's output actually reflecting the artefact, the question framing, or the option position it was given -- so none of them is offline-measurable by construction, and each is not counted toward the two measured weaknesses above. That is a statement about *what this file's own acceptance criterion counts*, not a reason to leave the numbers out of this file when they exist: relegating a check as serious as acquiescence bias to `docs/validity_report.md` alone, while this file's own opening line reads as a finished account of the panel's limits, is the same overclaim as not measuring it at all. Whenever `sul validate` had a real (or cassette-backed) provider available for a check below, its numbers -- with their denominators -- are printed here directly.

### Discriminative validity

{% if report.discriminative_validity.bad_blocker_confusion_count is not none %}
{% if report.discriminative_validity.status == PARTIALLY_MEASURED %}
**PARTIALLY MEASURED** -- {{ report.discriminative_validity.reason }}

{% endif %}
- Bad artefact blocker/confusion count: {{ report.discriminative_validity.bad_blocker_confusion_count }} ({{ report.discriminative_validity.bad_personas_completed }}/{{ report.discriminative_validity.bad_personas_attempted }} personas)
  - Category breakdown: {% for cat, count in report.discriminative_validity.bad_category_counts.items() %}{{ cat }}: {{ count }}{% if not loop.last %}, {% endif %}{% endfor %}{{ "\n" }}  - Distinct evidence anchors (turn ordinal, category): {{ report.discriminative_validity.bad_distinct_anchor_count }}
- Good artefact blocker/confusion count: {{ report.discriminative_validity.good_blocker_confusion_count }} ({{ report.discriminative_validity.good_personas_completed }}/{{ report.discriminative_validity.good_personas_attempted }} personas)
  - Category breakdown: {% for cat, count in report.discriminative_validity.good_category_counts.items() %}{{ cat }}: {{ count }}{% if not loop.last %}, {% endif %}{% endfor %}{{ "\n" }}  - Distinct evidence anchors (turn ordinal, category): {{ report.discriminative_validity.good_distinct_anchor_count }}
- Material difference: {{ report.discriminative_validity.material_difference }}
{% elif report.discriminative_validity.status == PARTIALLY_MEASURED %}
**PARTIALLY MEASURED** ({{ report.discriminative_validity.provenance|length }} provenance rows) -- {{ report.discriminative_validity.reason }}
{% else %}
**NOT MEASURED OFFLINE** -- {{ report.discriminative_validity.reason }}
{% endif %}

### Acquiescence bias

{% if report.acquiescence_bias.status == MEASURED or report.acquiescence_bias.status == PARTIALLY_MEASURED %}
{% if report.acquiescence_bias.status == PARTIALLY_MEASURED %}
**PARTIALLY MEASURED** ({{ report.acquiescence_bias.subjects_measured }}/{{ report.acquiescence_bias.subjects_attempted }} subjects completed both framings) -- {{ report.acquiescence_bias.reason }}

{% endif %}
- Positively framed question: {{ report.acquiescence_bias.positively_framed_question }}
- Negatively framed question: {{ report.acquiescence_bias.negatively_framed_question }}
- Positive-framing agree rate: {{ report.acquiescence_bias.positive_agree_rate }}
- Negative-framing agree rate: {{ report.acquiescence_bias.negative_agree_rate }}
- Agreement gap: {{ report.acquiescence_bias.agreement_gap }} ({{ report.acquiescence_bias.subjects_measured }}/{{ report.acquiescence_bias.subjects_attempted }} subjects) -- the panel agreed with whichever framing it was given, at this gap, over this denominator
{% else %}
**NOT MEASURED OFFLINE** -- {{ report.acquiescence_bias.reason }}
{% endif %}

### Position bias

{% if report.position_bias.status == MEASURED or report.position_bias.status == PARTIALLY_MEASURED %}
{% if report.position_bias.status == PARTIALLY_MEASURED %}
**PARTIALLY MEASURED** ({{ report.position_bias.subjects_measured }}/{{ report.position_bias.subjects_attempted }} subjects completed both orderings) -- {{ report.position_bias.reason }}

{% endif %}
- Options: {{ report.position_bias.options }}
- First-position share (original order): {{ report.position_bias.first_position_share_original_order }}
- First-position share (reversed order): {{ report.position_bias.first_position_share_reversed_order }}
- Preference shift: {{ report.position_bias.preference_shift }} ({{ report.position_bias.subjects_measured }}/{{ report.position_bias.subjects_attempted }} subjects)
{% else %}
**NOT MEASURED OFFLINE** -- {{ report.position_bias.reason }}
{% endif %}

### Known-answer calibration

{% if report.known_answer_calibration.detected_count is not none %}
{% if report.known_answer_calibration.status == PARTIALLY_MEASURED %}
**PARTIALLY MEASURED** ({{ report.known_answer_calibration.personas_completed }}/{{ report.known_answer_calibration.personas_attempted }} bad-artefact personas) -- {{ report.known_answer_calibration.reason }}

{% endif %}
- Detected: {{ report.known_answer_calibration.detected_count }} / {{ report.known_answer_calibration.total_defects }} ({{ report.known_answer_calibration.personas_completed }}/{{ report.known_answer_calibration.personas_attempted }} bad-artefact personas)
- Detection rate: {{ report.known_answer_calibration.detection_rate }}
{% elif report.known_answer_calibration.status == PARTIALLY_MEASURED %}
**PARTIALLY MEASURED** -- {{ report.known_answer_calibration.reason }}
{% else %}
**NOT MEASURED OFFLINE** -- {{ report.known_answer_calibration.reason }}
{% endif %}
{% if report.provider_name != "fake" %}

## These numbers describe one specific model configuration

`sul validate` picked up a real, cassette-backed recording for this run (PROJECT_SPEC.md §M6 Deviation 5, amended) rather than falling back to `FakeProvider` -- `tests/cassettes/` had traffic to replay. See the **Provenance** rows in `docs/validity_report.md` for exactly which model served each agent role (persona, moderator, analyst, probes) for each check.

**Those numbers describe that one configuration and do not transfer down a model tier.** A different Analyst model, or a different persona/moderator model, is a different measurement, not an extrapolation from this one -- nothing here should be read as validating a cheaper or different model combination than the Provenance rows actually show. One such swap has actually been measured, on this one 5-persona panel, one seed, one artefact pair: replaying the identical bad-artefact transcripts through a Haiku Analyst instead of Sonnet left the blocker/confusion aggregate and the material-difference verdict unchanged (6 vs 0 either way), and the difference between the two Analysts on this transcript is categorical, not about severity -- Haiku keeps the same two severity-4 findings Sonnet found but never labels one of them a `blocker` (relabelling it `confusion` instead) and shifts its middle mass down (mode severity 3 -> 2), anchoring evidence to 2 distinct transcript positions where Sonnet anchored to 5; on the clean artefact both Analysts report zero `blocker`/`confusion` findings for a well-supported reason, not a silent-Analyst artefact (100% `delight`, either config), but Haiku reported something for 5 of 5 personas where Sonnet reported for only 2, so Sonnet's bad-vs-good swing (8 findings -> 2) is driven by both volume and category while Haiku's (7 -> 5) is almost purely category, because it says something about the artefact regardless of whether it's good or bad -- at roughly a quarter the Analyst spend (`tests/test_validity_cassette_report_determinism.py::test_config_b_numbers_are_reproducible_and_complete`), on this transcript, this run, the headline metric did not register a real, material change in what the Analyst actually found, which is a property of this run, not a general claim about either model. See PROJECT_SPEC.md's §M6 Deviation 18-20 for what was recorded, what failed along the way, and what (if anything) remains open.
{% endif %}
