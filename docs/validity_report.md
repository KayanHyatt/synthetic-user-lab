# Validity report

Provider: `fake` / model: `fake-1`

Generated by `sul validate` (PROJECT_SPEC.md §M6). A section marked **NOT
MEASURED OFFLINE** could not be evaluated against this provider -- the reason
is given in that section, and no number is printed in its place.

## 1. Reproducibility

- Repeats: 3
- Finding count variance: 0.0
- Mean top-5 cluster Jaccard overlap: 1.0

> Zero variance and a 1.0 mean top-5 cluster Jaccard overlap here are structurally guaranteed by FakeProvider's deterministic hashing (derive_seed feeds a pure sha256-keyed synthesis, replayed identically for the same seed) -- this measures the harness's own determinism, not the panel's. A real provider run may show non-zero variance; that would be expected, not a failure (PROJECT_SPEC.md §M6.1: 'non-zero variance is fine; unreported variance is not').

### Seed sensitivity (addition beyond §M6.1's own requirement)

- Seeds tried: 3
- Finding count range: (10, 12)
- Cluster count range: (10, 12)
- Mean top-5 cluster Jaccard overlap across seeds: 0.0

## 2. Discriminative validity

**NOT MEASURED OFFLINE** -- FakeProvider synthesizes structured output (category, summary, agreement, choice) as a uniform-random function of the full prompt hash -- blind to artefact content, question framing, and option position. This check requires a real provider (a cassette-backed one is sufficient and stays offline); measuring it against FakeProvider would report sampling noise as if it were a result.

## 3. Acquiescence bias

**NOT MEASURED OFFLINE** -- FakeProvider synthesizes structured output (category, summary, agreement, choice) as a uniform-random function of the full prompt hash -- blind to artefact content, question framing, and option position. This check requires a real provider (a cassette-backed one is sufficient and stays offline); measuring it against FakeProvider would report sampling noise as if it were a result.

## 4. Position bias

**NOT MEASURED OFFLINE** -- FakeProvider synthesizes structured output (category, summary, agreement, choice) as a uniform-random function of the full prompt hash -- blind to artefact content, question framing, and option position. This check requires a real provider (a cassette-backed one is sufficient and stays offline); measuring it against FakeProvider would report sampling noise as if it were a result.

## 5. Known-answer calibration

Named "calibration" by PROJECT_SPEC.md §M6's own section header; the
criterion it specifies is recall against a fixed known-answer set ("Report
detection rate"), not confidence calibration.

**NOT MEASURED OFFLINE** -- FakeProvider synthesizes structured output (category, summary, agreement, choice) as a uniform-random function of the full prompt hash -- blind to artefact content, question framing, and option position. This check requires a real provider (a cassette-backed one is sufficient and stays offline); measuring it against FakeProvider would report sampling noise as if it were a result.

Total seeded defects: 3

## Offline-measurable weaknesses

These three feed `docs/limitations.md` directly -- unlike checks 2-5, none
of them need a real provider: they measure `sul.analysis.clustering` against
hand-built or deterministically-generated text, not against anything an LLM
produced.

### Clustering margin

- Near-duplicate max internal cosine distance: 0.4480
- Distinct-pair min cosine distance: 0.6447
- Margin: 0.1967
- `distance_threshold`: 0.6

### Zero-vector singleton conflation

- Zero-vector singleton clusters: 1
- Genuine frequency-1 singleton clusters: 1
- Total singleton clusters: 2

### Threshold scaling (`distance_threshold=0.6`, default unchanged)

| Finding count | Cluster count | Mean cluster size |
|---|---|---|
| 10 | 3 | 3.33 |
| 25 | 5 | 5.00 |
| 50 | 8 | 6.25 |
| 100 | 8 | 12.50 |
